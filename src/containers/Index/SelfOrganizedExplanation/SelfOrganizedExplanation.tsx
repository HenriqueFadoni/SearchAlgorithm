import React from 'react';

const SelfOrganizedExplanation: React.FC = () => (
  <>
    <p className="text-justify mx-5 mt-5">
      80% of the searches done are to retrieve 20% of our data. When we use self-organized algorithms,
      we bring data closer to the beginning of the collection. Even though it might not work for all the
      data in our dataset, this algorithm can be quite handy for frequently searched items.
    </p>
  </>
);

export default SelfOrganizedExplanation;